spring: 
  datasource: 
    url: jdbc:mysql://localhost:3306/libreria_v04?allowPublicKeyRetrieval=true&useSSL=false&useTimezone=true&serverTimezone=Europe/Madrid&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    # properties:
    #   hibernate:
    #     dialect: org.hibernate.dialect.MySQL5InnoDBDialect      
  thymeleaf:
    cache: false 
server:
  error:
    whitelabel:
      enabled: false


